let Scooter=function(year,color,model)
{
  this.year=year;
  this.color=color;
  this.model=model;
}

let Driver=function(name,age,experience)
{
  this.name=name;
  this.age=age;
  this.experience=experience;
  }
  
  let PickupLocation=function(address, city)
  {
    this.address=address;
    this.city=city;
  }